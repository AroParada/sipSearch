import { TextField } from "@mui/material";

export default function SearchBar() {
    return (
      <TextField
        id="outlined-basic fullWidth"
        label="Outlined"
        variant="outlined"
        fullWidth
      />
    );
}